<%= form_for [:admin, @user] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :email %>
    <%= f.text_field :email, {:id=>"Email"} %>
  </p>
  <p>
    <%= f.label :password %>
    <%= f.password_field :password, {:id=>"Password"} %>
</p>
  <%= f.submit %>
<% end %>